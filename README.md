# RC6-Encrypted-TCP-Distributed-System
A Python project that uses the Transfer Control Protocol (TCP) in order to send messages in a P2P architechture. The messages are encrypted using the RC6 algorithm.
